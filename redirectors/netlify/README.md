# Netlify 302 Redirector

Deploy this directory to Netlify to create a permanent streaming redirect domain.

### Deployment:
1. Import this folder into Netlify.
2. In `netlify.toml` or `_redirects`, replace `YOUR-STREAMING-FQDN.herokuapp.com` with your backend domain.
3. Attach your custom domain in Netlify Domain Management.
