services:
  filetolink:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: filetolink-go
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
